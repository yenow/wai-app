import 'package:flutter/cupertino.dart';

class SimplePersonalityTestPageScreen extends StatelessWidget {
  const SimplePersonalityTestPageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
