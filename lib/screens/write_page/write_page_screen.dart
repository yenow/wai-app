import 'package:flutter/cupertino.dart';

class WritePageScreen extends StatelessWidget {
  const WritePageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Wrap(
        children: const [Text('WritePageScreen')],),
    );
  }
}
